package com.app.opencallerid

import android.app.Application
import dagger.hilt.android.HiltAndroidApp

@HiltAndroidApp
class OpenCallerIDApplication : Application() {
    // Usually, this class can remain empty for basic Hilt setup.
    // You might add custom initialization logic in onCreate() later if needed.
}